version: '2'
services:
  main:
    build:
      context: ./workspace
      args:        
        - LOGIN=user
        - PASSWORD=password                
      dockerfile: "Dockerfile"    
    ports:
      - 48080:3128